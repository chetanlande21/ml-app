apiVersion: v1
kind: Service
metadata:
  name: ml-app
  labels:
    app: ml-app
spec:
  type: {{ .Values.service.type }}
  selector:
    app: ml-app
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 5000
